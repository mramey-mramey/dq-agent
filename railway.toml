[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build.nixpacksConfigOptions]
# Tell nixpacks to use Python 3.12
pythonVersion = "3.12"
